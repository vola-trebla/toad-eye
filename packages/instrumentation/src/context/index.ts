// Context module — post-call utilization monitoring only.
// Pre-call blocking was removed (see #198) — API providers handle
// context overflow natively, and pre-call token estimation is unreliable.
